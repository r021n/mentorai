<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Dashboard</title>
  </head>
  <body>
    <h1>MentorAI</h1>
    <% if (user) {%>
    <p>Selamat datang, <%= user.username %>!</p>
    <ul>
      <li>
        <a href="/exercise">Latihan</a>
      </li>
      <% if (user.role === 'admin') { %>
      <li><a href="/topics">Edit topic</a></li>
      <% } %>
    </ul>
    <a href="/logout">Logout</a>
    <% } else {%>
    <p><a href="/login">Login</a></p>
    <% } %>
    <nav>
      <li><a href="/faq">Tentang Kami</a></li>
    </nav>
    <button><a href="/exercise">Mulai</a></button>
  </body>
</html>
